#include <stdio.h>

int main()
{
    char *ptr;
    char s[100];

    int i;
    printf("enter the string : ");
    scanf("%s", s);
    for (i = 0; i < s[i] != '\0'; i++)
    {

        ptr[i] = s[i];
    }

    printf("%d", i);

    return 0;
}